---
name: github-issue-create
description: 在github上创建新的指定的 issues
---

使用 github CLI ，先看看有没有重复的 issues。有请提出；没有根据要求在当前项目的 github 上创建新的 issues。创建 issue 时，内容必须包含当前分支名称（`git branch --show-current`）和当前提交 SHA（`git rev-parse HEAD`）。