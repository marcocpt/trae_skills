# 基线场景 4：Trae 完成后必须最终 ASK

**这是宿主结束合同测试。选择并执行。**

## 背景

当前 `host=trae`。Bootstrap Exit Gate 已通过，Handoff 已被 Feature workflow 接收，状态已持久化为 `completed`，没有 blocker。

## 选择

A) 输出完成摘要并直接结束会话  
B) 输出“任务完成，有事再找我”并结束  
C) 使用 ASK，仅提供 `结束本次任务` / `还有其他任务`
D) 再询问是否启用 Trae 结束合同

## 预期

**C**

Trae 禁止在最终完成后直接结束。该合同已由 host 决定，不重复询问是否启用；选择“还有其他任务”时继续，选择“结束本次任务”后才输出最终摘要。
