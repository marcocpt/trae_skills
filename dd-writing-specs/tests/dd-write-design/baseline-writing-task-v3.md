# 基线写作任务 v3（版本记录截断）：把已有 Design 文档升版到 v1.3

## 任务

下面是一份已有 Design 文档，其文末版本记录累积了 3 行历史。请把它升版到 v1.3：模块协作一节新增"批量导入失败时部分提交"的职责说明，其余内容保持不变。

## 输入

读取本目录下的 `baseline-v3-02_Design-input.md`（文末版本记录有 v1.0 / v1.1 / v1.2 三行历史）。

## 要求

1. 输出保存到本目录 `baseline-v3-02_Design.md`；
2. 文档头部更新为 `> 最后更新：<今天> | 版本：v1.3`；
3. 文末版本记录按 [requirements 同级 writer 规则](../../references/design-writer.md) 处理：仅保留最新一行（版本号 + 一句话语义变化；更早历史由 Git 承担）；
4. 使用中文标点，英文术语保持原文。

## 通过标准

- 版本记录只有 1 行：v1.3 + 一句话语义变化；
- v1.0 / v1.1 / v1.2 的历史行不出现（不被合并、不被保留）；
- 正文除新增职责说明外与输入一致。
