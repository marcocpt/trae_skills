# AGENTS.md

本仓库用于维护可复用的 Agent Skills。修改现有 Skill 或新增 Skill 时，优先保持既有架构、单一事实源、渐进式加载和合同测试，不以缩短文件或减少 token 为由降低可执行性与质量门禁。

## 1. 修改前必须先读什么

开始修改前，按最小必要范围读取：

1. 本文件；
2. 目标 Skill 的 `SKILL.md`；
3. 该 `SKILL.md` 实际引用、且与当前修改有关的 `references/`；
4. 目标 Skill 的现有测试（若有）；
5. 若修改涉及共享状态、恢复、审查、验证、CI、证据或交付语义，再读取 `dd-workflow-runtime` 对应 reference。

不要默认预加载整个仓库、全部 reference、历史计划或无关 Skill。

已有事实优先从仓库、状态、已批准文档和测试中恢复；能从现有证据确定的内容不要重新询问。

---

## 2. 顶层 Skill 的边界

顶层 Skill 应对应：

* 用户可直接表达的任务意图；或
* 一个可独立触发、具有完整入口和退出条件的工作流。

以下内容通常不应新建为顶层 Skill：

* 单个阶段；
* 共享 schema；
* 状态或恢复机制；
* Git 原子操作；
* CI 通用规则；
* 工作流内部的审查协议或传输协议（面向用户独立触发的审核闭环 Skill，如 `gpt-grilling-review`，不在此列）；
* 模板；
* 仅供其他 Skill 调用的局部实现步骤。

这些内容优先放入现有 Skill 的 `references/`，或由已有共享 Skill / runtime 统一拥有。

新增顶层 Skill 前，先确认现有 Skill 无法通过扩展或 reference 路由承载该意图。

---

## 3. `SKILL.md` 与 reference 的职责

### 编排型 Skill

工作流或编排型 `SKILL.md` 应采用 **Thin Executable Router**：

主 `SKILL.md` 负责让 Agent 快速理解：

* 这个 Skill 解决什么问题；
* 何时适用、何时不适用；
* 整体按什么顺序执行；
* 当前阶段实际要做什么；
* 每一步得到什么或怎样算完成；
* 什么时候需要打开哪个 reference；
* 哪些跨阶段红线始终不能违反。

reference 负责：

* 具体执行算法；
* schema 和字段定义；
* 模板；
* 异常分支；
* 恢复细节；
* 风险触发条件；
* CI / CLI 具体命令；
* 完整检查表；
* 长示例。

**Thin 不等于抽象。**

如果 Agent 只读主 `SKILL.md` 后仍只能看到大量阶段、Gate、状态名和术语，却不知道"接下来实际做什么"，则主文件过薄。

如果主文件开始复制 reference 的详细规则，则主文件过厚。

### 非编排型 Skill

不要为了统一形式强行加入阶段、Gate 或流程图。

参考型、工具型、方法型 Skill 按自身任务结构组织，但仍必须保持：

* 可快速理解；
* 规则属主明确；
* 细节按需加载；
* 不重复其他单一事实源。

---

## 4. 单一事实源

每条规范性规则只允许有一个详细属主。

修改规则前先找到当前属主。

原则：

* 上游拥有事实，下游引用事实；
* 消费方可以做一句话概述，但不得复制完整定义；
* schema、字段语义、枚举、状态机、审查等级、CI 判定等共享合同由其现有属主维护；
* 派生产物可以展开执行材料，但不能成为新的权威事实源；
* 历史计划、迁移记录和验证证据不是当前规则的替代品。

发现两个 active 文件同时完整定义同一规则时，应优先保留既有权威属主，并把其他位置改为概述和链接。

不要为了"自包含"制造第二套规则。

---

## 5. 修改现有 Skill 的纪律

修改现有 Skill 时：

1. 保持任务范围最小，不顺手修改无关 Skill；
2. 不重新设计用户未要求改变的工作流；
3. 不删除、降低或改变已有 Gate，除非任务明确要求改变合同；
4. 不因文案重构修改现有 schema、字段名、枚举值、状态值、CLI 参数或合同标识；
5. 不通过重命名规避现有测试或兼容性要求；
6. 不把已下沉到 reference 的详细规则重新复制回主文件；
7. 不以"文件更短"作为成功标准；
8. 不以"省 token"为理由删除必要上下文、验证、证据或恢复语义；
9. 不宣称 token 一定下降，除非存在实际测量；
10. 修改共享规则时检查所有直接消费者是否仍成立；
11. 发现现有合同相互矛盾时，记录 finding，不擅自选择新语义；
12. 保持现有渐进式加载路径有效。

结构重构的目标应优先是：

> 更容易正确执行，而不是单纯更短。

---

## 6. 新增 Skill 的要求

新增 Skill 前确认：

* 当前不存在等价用户意图的 Skill；
* 不能合理作为现有 Skill 的 reference；
* 不是仅为了一个阶段、命令或模板建立新入口；
* `name` 和 `description` 能让 Agent 正确判断何时加载；
* `description` 主要描述触发条件，不用它替代正文工作流；
* 主文件只包含第一次执行真正需要的内容；
* 大量细节按需拆入 reference；
* 必要的失败场景和合同测试已经设计。

能用机械 validator、schema 或测试强制执行的规则，优先机械化；文档主要约束需要判断的行为。

详细 Skill 编写和测试方法使用 `writing-skills/SKILL.md`。

---

## 7. 中文表达与原始标识

仓库正文默认使用自然、准确的中文。

能准确用中文表达的概念，应优先使用中文，不要为了显得专业而夹杂英文单词，也不要无必要地使用中英混排。

尤其避免以下写法：

* "执行 evidence"；
* "做 review"；
* "进入 delivery"；
* "检查 runtime state"；
* "做 full-spec gap 检查"；
* "冻结 frozen candidate"。

应优先改写为：

* "保存验证证据"；
* "执行审查"；
* "进入交付阶段"；
* "检查运行时状态"；
* "执行完整规格缺口检查"；
* "冻结候选版本"。

同一句话中，如果英文内容不是精确标识、专有名词或不可替代的技术简称，应优先改写为中文。

以下内容必须保持原始标识，不得为了中文化而修改：

* Skill 名；
* 文件名和路径；
* 代码符号、API、类名、函数名；
* schema 字段；
* YAML / JSON key；
* 枚举值；
* 状态值；
* 稳定 ID；
* CLI 命令和参数；
* 已存在的合同字段和合同标识；
* 外部产品、框架、协议的正式名称；
* 已形成稳定技术含义且翻译会降低准确性的简称，例如 `TDD`、`CI`。

必要时可以在原始标识第一次出现时补充中文解释，但不得修改标识本身。

例如：

* `candidate_sha`：保持字段名不变，正文可称"候选 SHA"；
* `delivery_authorization`：保持字段名不变，正文可称"交付授权"；
* `full_ci_run`：保持字段名不变，正文可称"完整 CI 运行记录"。

原则：

> 中文正文尽量使用中文；英文只用于精确标识、必要技术简称和正式专有名词，不作为普通叙述语言。

---

## 8. 共享治理边界

以下通用语义应继续由现有属主维护，不在其他 Skill 中重新定义：

* 状态、恢复、Stage Contract、Host Close：`dd-workflow-runtime`；
* 通用 artifact 来源、验证、生命周期合同：`dd-workflow-runtime/references/artifact-contract.md` 及其分文件；
* A/B/C 审查语义和风险路由：`dd-workflow-runtime/references/review-gate.md`；
* 审查等级与强弱模型路由：`dd-workflow-runtime/references/model-routing.md`；
* ChatGPT 外部审核闭环与传输：`gpt-grilling-review`；
* 通用 CI / test-location：`dd-workflow-runtime/references/` 对应文件；
* Git 操作：`dd-git-workflow`；
* Requirements / Design / Visual / Test Matrix 写作语义：`dd-writing-specs`；
* Skill 编写方法和 Skill TDD：`writing-skills`。

领域 Skill 只增加自己的领域规则，不复制这些共享合同。

---

## 9. 验证

修改后至少执行：

1. 目标 Skill 已有的合同测试；
2. 如果修改共享 runtime，则执行受影响的 runtime 测试；
3. 如果修改共享合同，则执行其消费者中已有的相关合同测试；
4. `git diff --check`。

同时人工检查：

* 所有 active reference 路径仍有效；
* 没有新增第二套单一事实源；
* 没有意外修改任务范围外文件；
* 阶段、Gate、状态语义没有被文案重构意外改变；
* 没有通过删除或弱化断言让测试"变绿"；
* 主 `SKILL.md` 与 reference 的职责仍清晰；
* 新增内容值得其上下文和维护成本；
* 正文没有新增无必要的中英混排。

测试因真实合同变化需要修改时，必须保留原合同保护目的，并说明为什么旧断言不再正确。

---

## 10. Git 与外部动作

工作流内容完成、测试 PASS、审查 PASS 与 Git / 外部动作授权是不同概念。

不得把：

* 文档批准；
* Workflow Gate PASS；
* 测试 PASS；
* Reviewer PASS；

自动解释为 commit、push、merge、PR、远端 CI、发布或其他外部动作授权。

具体动作继续遵循用户当前要求、项目规则和对应 workflow / Git 合同。

---

## 11. 红线

禁止：

* 为一个局部阶段创建新的顶层 Skill；
* 建立与现有 runtime 平行的状态系统、ledger 或共享工作流；
* 复制已有 canonical schema 或状态机；
* 为了可读性把完整 reference 塞回 `SKILL.md`；
* 为了省 token 删除质量 Gate；
* 为了通过测试加入无语义关键词；
* 把严格合同测试改成无意义的字符串存在检查；
* 修改任务无关 Skill；
* 把历史计划当作当前单一事实源；
* 在发现合同冲突时静默改变既有语义；
* 在正文中无必要地混用中英文表达；
* 为了中文化修改字段名、枚举值、代码符号或其他稳定合同标识。

目标始终是：

> Agent 首次读取入口文件即可理解当前任务该怎么走；进入具体步骤时，再加载该步骤唯一的详细规则。
