# 步骤 0 规则与参考摘要

> 目标:修改 writing-design-specs/SKILL.md(检查并修改该技能本身)
> 日期:2026-07-06

## 0.1 项目规则文件状态

- `.trae/rules/docs.md`:**不存在**
- `docs/CODING_STANDARDS.md`:**不存在**
- `docs/AI/trae-xctest-rules.md`:**不存在**

**docs.md 6 项提取结果(因文件不存在,使用默认规则)**:
1. 设计规范必须包含的章节:使用默认 12 章节(见 SKILL.md 步骤 2.2)
2. 文档编写顺序:设计规范 → 视觉原型(如涉及 UI) → 测试用例表
3. 文件命名规则:`F{N}_{功能名}_设计规范.md`(默认)
4. 文档存放路径:`docs/superpowers/specs/`(默认,无项目规则)
5. 版本号和最后更新日期规则:文档头部 `> 最后更新:YYYY-MM-DD | 版本:vX.Y`,每次修改同步更新
6. 是否要求其他文档:默认要求设计规范;视觉原型/测试用例表按需

**用户对"docs.md"的澄清(2026-07-06)**:
用户要求"检查文档时,必须至少有一个子代理检查文档是否符合 docs.md"。
项目无 .trae/rules/docs.md。用户澄清:"docs.md"指 **writing-design-specs/SKILL.md 文档中提到的 docs.md 概念**。
→ 审查维度之一:检查修改后的 SKILL.md 中关于 docs.md 的描述是否自洽、一致、符合其自身定义的规则(元一致性检查)。

## 0.2 app 功能列表状态

- `app 功能列表.md`:**不存在**
- `docs/app 功能列表.md`:**不存在**
- `docs/app功能列表.md`:**不存在**

**项目性质**:skills 项目是技能集合,每个技能一个 `SKILL.md`,无传统 app 功能编号。
**本次修改目标**:writing-design-specs/SKILL.md(修改该技能本身)
**功能编号**:需在 grill 中确认(建议用 `F0` 或 `meta` 表示元修改)
**优先级**:需在 grill 中确认

## 0.3 既有设计规范参考

- `docs/planning/` 下无 `*设计规范*.md`
- `docs/` 下无 `*设计规范*.md`
- 项目无任何既有设计规范文件

**可参考的"既有规范"**:其他技能的 SKILL.md(风格一致性参考)
- 已完整阅读:writing-design-specs/SKILL.md(待修改目标)
- 风格特征:中文 + 英文术语混用、章节编号清晰、表格密集、有 `<HARD-GATE>`/红线/合理化借口表等结构、Conventional Commits 风格
- 可复用写作模式:章节层次、表格格式、违规补救路径表、合理化借口表

## 备注

本次任务是元操作:用 writing-design-specs 流程修改 writing-design-specs/SKILL.md 自身。
- 步骤 2 "写规范"在此场景下指:产出修改后的 writing-design-specs/SKILL.md
- 步骤 3 "审查"中至少一个子代理检查:修改后的 SKILL.md 中关于 docs.md 的描述是否自洽一致
- 由于目标文档本身就是规则文档,需注意自洽性(修改不能破坏现有规则体系)
