# 技术调研自检记录

> 最后更新：2026-08-27 | 版本：v0.2 | 结论：本地实施与行为复测通过，收益待评测

- 对象：`技术调研.md` v0.2；SHA-256：`c725d9c579a467291e313e90b52d979cedfe7ce6d17856876537ab4e66536d4c`。
- 实施基线：分支 `docs/streamline-doc-system-research`，HEAD `e84eca2170e84bd907491649567b65a27a68efa2`。
- 边界：只修改本 worktree 的文档技能体系；未安装外部工作流，未执行 Git 交付。

## 实施结果

| 项目 | 结果 | 证据位置 |
|---|---|---|
| R1 原文反查 | PASS | `dd-feature-development-workflow/references/implementation-and-verification.md` 在 Phase 与 Final Candidate 重读批准原文并重建 gap table |
| R2 唯一维护 | PASS | 通用 A/B/C 只由 Runtime `review-gate.md` 定义；Specs reference 仅保留文档特有检查 |
| R3 确认／交付分离 | PASS | `artifact-contract.md` 分开定义 `approval` 与 `delivery_authorization`；Specs 允许同一 worktree 在禁止 Git 时继续 |
| R4 覆盖／通过分离 | PASS | 唯一合同区分四层证据，含 `UNVERIFIED`、`NOT_RUN`、`stale` 和 PASS 条件 |
| D1–D7 | PASS | 新增一个共享 reference 并由既有 Runtime、Project Docs、Specs、Feature 路由；未新增顶层 skill、状态系统或生成器 |

## 已执行检查

- 四个改动 skill 均通过 `skill-creator/scripts/quick_validate.py`；`git diff --check` 与暂存区空白检查通过。
- 19 份改动／新增 Markdown 通过末尾换行、行尾空白、冲突标记、本地链接和关键不变量检查；摘要 188／300 字符。
- 弱模型红测：R2=`PARTIAL`、R3=`FAIL`、R1／R4=`PARTIAL`，失败原因已保存为三个 baseline 场景。
- 首轮绿测：R2、R3 通过；R1／R4 场景发现“不可核对覆盖”和授权结构两处歧义。补强后，用两个全新弱模型实例复测，均正确选择 `BLOCKED`／继续动作和结构化状态，无合理绕过。
- 共享合同初稿由 4913 压缩到 3775 bytes；但 14 份相关生产指令相对 HEAD 仍净增 11882 bytes。该数字只描述规则源文本，不代表端到端 token 成本。
- v0.1 调研时已核对 15 个固定版本外部来源为 HTTP 200、8 个本地来源与当时基线一致；本轮未重复请求未变化的外部来源。

## 未执行与结论边界

- `NOT_RUN`：同一模型／同一真实开发任务的端到端 token、约束违规、AC 遗漏和返工成本对照；因此不宣称已省 token 或已证明质量收益。
- `NOT_RUN`：跨宿主及完整 Feature 实战；本轮行为测试是隔离的文档规则压力场景。
- `EXTERNAL_REVIEW_NOT_EXECUTED`：未获准外发仓库源码、diff 或上下文，未调用外部 ChatGPT 审查。
- 暂存区为空；未 commit、push、merge、创建 PR 或修改原工作树的既有用户改动。
