# 技术调研自检记录

> 最后更新：2026-08-27 | 版本：v0.3 | 结论：生命周期合同绿测通过，收益待评测

- 对象：`技术调研.md` v0.3；来源指纹：`artifact-contract.md` 当前 76fb7eb（Task 2 修复后）/ 原绿测基线 422f6ae，5 个 baseline 文件均更新至绿测 PASS 并补齐变体 provenance（见下，修复后 HEAD a360406）。
- 实施基线：分支 `docs/streamline-doc-system-research`，HEAD `a360406`（含 Task 1 绿测与 Task 2 F-01/F-02 修复，基于 627104e）。
- 边界：只修改本 worktree 的文档技能体系；已在隔离分支 Git 交付 Task 1/2/3（含 provenance 补齐），未 push，未安装外部工作流。

## 实施结果

| 项目 | 结果 | 证据位置 |
|---|---|---|
| R1 原文反查 | PASS | `dd-feature-development-workflow/references/implementation-and-verification.md` 在 Phase 与 Final Candidate 重读批准原文并重建 gap table |
| R2 唯一维护 | PASS | 通用 A/B/C 只由 Runtime `review-gate.md` 定义；Specs reference 仅保留文档特有检查 |
| R3 确认／交付分离 | PASS | `artifact-contract.md` 分开定义 `approval` 与 `delivery_authorization`；Specs 允许同一 worktree 在禁止 Git 时继续 |
| R4 覆盖／通过分离 | PASS | 唯一合同区分四层证据，含 `UNVERIFIED`、`NOT_RUN`、`stale` 和 PASS 条件 |
| D1–D7 | PASS | 新增一个共享 reference 并由既有 Runtime、Project Docs、Specs、Feature 路由；未新增顶层 skill、状态系统或生成器 |
| L1 生命周期 7 类 | PASS | `artifact-contract.md` §3 唯一定义 canonical/active-change/derived/evidence/decision/working/closed-change，11 消费方仅引用 |
| L2 同步影响 5 种 | PASS | `updated`/`no-update`/`stale`/`not-applicable`/`retired` 与四类路由已在 Feature/Bug/Refactor 落地 |
| L3 保留与台账 | PASS | `retention.yaml`/`ledger.md` 最小字段与 unique_evidence 保护已固化 |
| L4 派生与 INDEX | PASS | trace_map/执行包/INDEX 为 derived，stale 重派生，INDEX 同一 change set 仅 Delivery 授权时同 commit |

## 已执行检查

- 实际命令与结果（2026-08-27）：
  - `git diff --check`：PASS（无尾随空白、冲突标记）
  - `grep -rn canonical` 在 artifact-contract 命中 7 类定义；`grep -rn 必须提交` 仅命中条件提交说明（获 Delivery 授权时同 commit），无无条件硬编码
  - 弱模型红测（Task 1）：5 场景均 FAIL，已保存 5 个 baseline 文件（luna/nova/atlas/vega/polaris 等 worker，含 prompt 指纹与原话）
  - 弱模型绿测（Task 3）：5 场景均 PASS（phoebe/cassie/mira/orion/lyra/sirius/capella/polaris 等全新实例，含读取规则与 prompt 指纹），3 个变体（closed Phase 开放 evidence / derived 新内容 / 禁止 Git）均 PASS，无新增合理化漏洞，记录 `no-gap`
  - 元测试：针对 Task 1 红测失败场景做规则组织复核（绿测阶段无仍失败场景，故记 NOT_APPLICABLE），确认仅需 Task 2 已实施的 12 文件最小修改，无新增顶层 skill/生成器
  - 8 个受影响 skill 的 `quick_validate.py`：路径 `~/.codex/skills/.system/skill-creator/scripts/quick_validate.py` 不存在（NOT_FOUND），改用 `git diff --stat` 确认 12 modified / 0 added / 0 deleted 且无新顶层 skill/生成器/token 阈值
- 19 份改动／新增 Markdown 通过末尾换行、行尾空白、冲突标记、本地链接和关键不变量检查；摘要 188／300 字符。
- 共享合同由 52 行扩展至 130 行（含 §3 生命周期与保留），但仅新增必要字段，未引入固定 token/行数阈值。
- v0.1 调研时已核对 15 个固定版本外部来源为 HTTP 200、8 个本地来源与当时基线一致；本轮未重复请求未变化的外部来源；当前 plan 来源指纹：artifact-contract 等 12 文件已按 Task 2 修复后提交 76fb7eb，Task 3 绿测与变体 provenance 补齐后为 a360406。

## 未执行与结论边界

- `NOT_RUN`：同一模型／同一真实开发任务的端到端 token、约束违规、AC 遗漏和返工成本对照；因此不宣称已省 token 或已证明质量收益（本次 green 5/5 PASS 仅证明约束有效性，不推导 token 节省）。
- `NOT_RUN`：跨宿主及完整 Feature 实战；本轮行为测试是隔离的文档规则压力场景。
- `NOT_RUN`：8 项 skill 的 `quick_validate.py` 官方脚本未在本地找到，未能机械执行；已用 `git diff --stat` 与 `grep` 替代，未宣称全部 validator PASS。
- `EXTERNAL_REVIEW_NOT_EXECUTED`：已执行内部 ChatGPT 审核（Task 0/1/2  via cpdf-review 会话），但受限于 Tunnel 读取失败，结论为 VERIFICATION_REQUIRED，待本地机械复核后进入下一 Task；未外发源码/diff，仅发送仓库标识与审核要求。
- 当前分支已提交 Task 1（2a0b33f）、Task 2（627104e → 76fb7eb 修复）与 Task 3 绿测及 provenance 补齐（422f6ae → a360406）；未 push、merge、创建 PR 或修改原工作树的既有用户改动。
